# Lane D, ressources, blog, articles, Legal Data

25 pages : 10-ressources, 11-blog, 12-article, 13-glossaire, 14-terme, 15-evenements,
16-evenement, 17-modeles, 18-recrutement, 19-poste, 22-legal, 23-research (+ en),
24-blog (+ en), 25-skills (+ en), 26-legal-data (+ en), 27-article-levee (+ en),
28-article-vivatech (+ en), 29-article-deel (+ en). Plus `commun/lanes/ressources.css`.

Cette session reprend le travail d'une lane D coupée à 11:18 avant son rapport.
Ses éditions étaient sur le disque : je les ai relues au `git diff`, vérifiées à
l'image, complétées, et c'est l'ensemble qui est mesuré ci-dessous.

Trois états sont en jeu et je les nomme séparément, parce que je n'ai pu en
remesurer que deux :

- **AVANT-1**, le site servi, avant le socle : chiffres transmis dans le brief,
  disponibles pour quatre pages seulement. **Je ne peux pas les revérifier** :
  cet état n'existe plus sur le disque. Je les cite comme reçus.
- **AVANT-2**, 11:19, après les éditions de la lane précédente et AVANT les
  corrections de système de 11:25 et 11:34 : `rapports/lane-D/capture-finale.log`,
  les 25 pages.
- **APRÈS**, ma dernière capture : `rapports/lane-D/capture-0309-verif.log`.

---

## Mesures AVANT / APRÈS

Desktop 1440 px, téléphone 390 px. « txt<14 » et « cibles<40 » sont relevés sur
téléphone, le reste sur desktop. Le pied est passé de 525 à 517 px partout : c'est
une correction de système de l'orchestrateur, pas de moi.

| page | h1 AVANT-2 → APRÈS | desktop AVANT-2 → APRÈS | mobile AVANT-2 → APRÈS | txt<14 AVANT-2 → APRÈS | cibles<40 AVANT-2 → APRÈS | liens morts | img sans alt | débord. | para le + large |
|---|---|---|---|---|---|---|---|---|---|
| 10-ressources | 40 → 40 px | 2711 → 2703 | 5724 → 5426 | 0 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 11-blog | 40 → 40 px | 4150 → 4142 | 9828 → 9634 | 0 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 12-article | 40 → 40 px | 6250 → 6224 | 9559 → 9368 | 0 → 0 | 0 → 0 | 0 | 0 | non | 580 |
| 13-glossaire | 40 → 40 px | 2617 → 2543 | 5291 → 4980 | 0 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 14-terme | 40 → 40 px | 3093 → 3085 | 5195 → 4981 | 3 → 0 | 0 → 0 | 0 | 0 | non | 580 |
| 15-evenements | 40 → 40 px | 3080 → 3072 | 6906 → 6515 | 0 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 16-evenement | 40 → 40 px | 2676 → 2668 | 5133 → 5022 | 4 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 17-modeles | 40 → 40 px | 2710 → 2702 | 6222 → 6028 | 0 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 18-recrutement | 40 → 40 px | 3020 → 3012 | 5667 → 5359 | 0 → 0 | 0 → 0 | 0 | 0 | non | 600 |
| 19-poste | 40 → 40 px | 3801 → 3793 | 6756 → 5936 | 2 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 22-legal | 40 → 40 px | 2798 → 2790 | 4136 → 4131 | 1 → 0 | 0 → 0 | 0 | 0 | non | 560 |
| 23-research | 40 → 40 px | 5403 → 5395 | 8481 → 8453 | 9 → 0 | 2 → 0 | 0 | 0 | non | 560 |
| 23-research-en | 40 → 40 px | 5385 → 5377 | 8257 → 8222 | 9 → 0 | 3 → **1** | 0 | 0 | non | 560 |
| 24-blog | 40 → 40 px | 6120 → 6112 | 11432 → 11287 | 3 → 0 | 2 → 0 | 0 | 0 | non | 560 |
| 24-blog-en | 40 → 40 px | 5934 → 5926 | 11132 → 11011 | 3 → 0 | 3 → **1** | 0 | 0 | non | 560 |
| 25-skills | 40 → 40 px | 6383 → 6375 | 11869 → 11779 | 11 → 0 | 2 → 0 | 0 | 0 | non | 688 |
| 25-skills-en | 40 → 40 px | 6271 → 6263 | 11592 → 11550 | 11 → 0 | 3 → **1** | 0 | 0 | non | 688 |
| 26-legal-data | 51,84 → 51,84 px | 6777 → 6825 | 10702 → 10567 | 6 → 0 | 2 → 0 | 0 | 0 | non | 560 |
| 26-legal-data-en | 51,84 → 51,84 px | 6739 → 6787 | 10591 → 10480 | 6 → 0 | 3 → **1** | 0 | 0 | non | 560 |
| 27-article-levee | 40 → 40 px | 3809 → 3801 | 5250 → 5101 | 2 → 0 | 2 → 0 | 0 | 0 | non | 580 |
| 27-article-levee-en | 40 → 40 px | 3771 → 3763 | 5053 → 4928 | 2 → 0 | 3 → **1** | 0 | 0 | non | 580 |
| 28-article-vivatech | 40 → 40 px | 3753 → 3745 | 5130 → 4981 | 2 → 0 | 2 → 0 | 0 | 0 | non | 580 |
| 28-article-vivatech-en | 40 → 40 px | 3614 → 3606 | 4971 → 4846 | 2 → 0 | 3 → **1** | 0 | 0 | non | 580 |
| 29-article-deel | 40 → 40 px | 4379 → 4371 | 6133 → 5984 | 2 → 0 | 2 → 0 | 0 | 0 | non | 580 |
| 29-article-deel-en | 40 → 40 px | 4192 → 4184 | 5889 → 5764 | 2 → 0 | 3 → **1** | 0 | 0 | non | 580 |

**Total : 68 textes sous 14 px et 40 cibles sous 40 px à 11:19 → 0 texte et 7 cibles.**
Les 7 cibles restantes sont un seul et même lien, le même sur les sept pages
anglaises, et il est hors de mon territoire (voir plus bas).

Zéro lien mort (`href="#"`), zéro champ figé (`input disabled`), zéro image sans
`alt`, zéro débordement horizontal, zéro erreur console, `Satoshi` sur les 25 pages,
`<main>` et le lien d'évitement présents partout.

### AVANT-1, ce qui est comparable

| relevé transmis (site servi) | APRÈS, mesuré |
|---|---|
| 26-legal-data : 215 textes < 14 px, 49 cibles < 40 px | 0 et 0 |
| 24-blog : 71 textes < 14 px, 44 cibles, 18 images sans alt | 0, 0 et 0 |
| 12-article : titre de 250 caractères | 62 caractères, `t-titre-page` 40 px |
| 23-research / 25-skills : h1 à 31,68 px | 40 px, `t-titre-page`, sur les deux |

Le poids HTML de 24-blog (3,4 Mo transmis) n'est pas comparable : la maquette ne
sert pas le même corpus (16 cartes contre 114 articles). Je ne le reporte pas.

### Mesure de lecture, recomptée

Comptée le 03/09 à 1440 px, caractère par caractère au `Range`, sur les trois
premiers paragraphes longs de chaque page, avec un témoin : la largeur rendue du
bout de ligne compté doit tenir dans la colonne, elle y tient partout.

| famille | colonne | corps | caractères par ligne |
|---|---|---|---|
| `.article p` (12, 14, 19, 27, 28, 29) | 580 px (524-530 en encart) | 18 px | **60 à 75**, médiane 68 |
| `.legal p` (22) | 500 px | 16 px | **67 à 73** |
| `.res-corps p.t-body` (23, 26) | 500 px | 15 px | **76 à 84** |

Les deux premières familles sont dans la bande visée (75). La troisième dépasse
de neuf caractères : c'est un chapô de section, pas du texte suivi.

**Correction à porter au crédit de la mesure, pas de la lane précédente** : le
commentaire de `ressources.css` annonçait « 18 px sur 580 px rend 82 caractères,
mesurés ». C'était une extrapolation à 0,39 em par caractère. La mesure réelle
sur cette matière donne 0,47 em, soit 60 à 75. J'ai corrigé le commentaire dans
le fichier pour qu'il porte le chiffre mesuré.

---

## Ce qui a été changé

### Par la lane précédente (relu au `git diff`, vérifié à l'image)

| fichier | ce qui change | pourquoi | vérification |
|---|---|---|---|
| les 25 pages | classe `gc-deep` retirée des sections en champ profond | classe morte | `gc-deep` n'est définie que dans `commun/composants-noir.css.sauvegarde`, jamais chargé ; `base.css:147` donne le fond à `.sur-sombre` seul. Le champ profond est intact à l'image |
| `ressources.css` | `.article` 720 → 580 px, corps 16 → 18 px ; `.legal` 500 px / 16 px ; `.chapo` 500 px | la ligne courait à plus de 100 caractères | recomptée : 60-75 et 67-73 car. (tableau ci-dessus) |
| `ressources.css` + 26-legal-data (+ en) | `.atlas-matrice`, colonne des libellés collante, note « le tableau défile » | à 390 px les 16 colonnes de marchés n'avaient plus de ligne d'ancrage | `position: sticky` confirmé au calcul ; visible sur `26-legal-data-m3.png` |
| 26-legal-data (+ en) | retrait des 3 pseudo-filtres « Tous / Surveillés seulement / Angles morts » et du bouton « Charger plus » | trois `<span>` inertes et un bouton qui pointait sur sa propre ancre | plus aucun `href="#"` ni faux contrôle sur la page |
| 26-legal-data (+ en) | « Affichés 60 sur 5 613 SH6 » → « Affichés 6 sur 5 613 codes SH6 » | le tableau porte 6 lignes | 6 lignes comptées dans le DOM |
| 25-skills (+ en) | 45 noms de skills : bleu `--c-signal` → encre (`.skill-nom`) | le bleu est la couche signal, ces noms ne sont ni lien ni bouton | plus aucun bleu en dur dans le territoire |
| 17-modeles | 6 `<span class="cta-texte">` sans `href` → `<a href="mailto:…">`, « Télécharger » → « Demander le modèle » | une action morte, et aucun fichier à télécharger n'existe | 6 liens vivants, libellé honnête |
| 17-modeles | 6 renvois réglementaires bleus → `.ref-texte` en encre | même raison que les skills |, |
| 12-article | titre bicolore retiré du h1 | `t-titre-page` n'est pas un hero | doctrine `base.css:128-132` |
| 24-blog (+ en) | h1 `t-hero` → `t-titre-page` ; 5 signatures d'auteur retirées sur 16 cartes | deux anatomies de carte dans une même grille | les 3 signataires et leurs volumes restent dans « Le corpus » : Naomie Halioua 81, Anaelle Guez 25, Alexandre Bloch 8. Aucune information perdue |
| 22-legal | `hello@cleolabs.co` → `contact@cleolabs.co` | cohérence | 23 occurrences de `contact@` sur le site, 0 de `hello@` |
| 22-legal | `.legal-tete` 640 px, fond de la note en `var(--c-signal-wash)` | chapô à 1 040 px |, |
| 14-terme, 19-poste | `.fil-ariane` (cible 44 px), `.renvois` (pilules 44 px) | fil d'Ariane à 78 × 15 px | 0 cible sous 40 px sur ces pages |
| 16-evenement | `.form-evenement` : libellés de formulaire à 14 px sur téléphone | ils rendaient 12 px | 0 texte sous 14 px |
| 18-recrutement | `.liste-postes` en classe ; `opacity:0.75` retirée de la candidature spontanée | une ligne de liste à demi effacée sans raison d'état |, |
| 24-blog, 25-skills, 26-legal-data (+ en) | chapôs bornés par `.chapo` au lieu de 5 valeurs inline différentes |, | `paragrapheLePlusLarge` ≤ 688 px partout |

**Jumeaux FR/EN** : vérifiés bloc à bloc en comparant la séquence complète des
attributs `class` des deux fichiers. Identiques sur les 7 paires :
23-research 121/121, 24-blog 147/147, 25-skills 161/161, 26-legal-data 410/410,
27 18/18, 28 20/20, 29 18/18.

### Par moi

| fichier | ce qui change | pourquoi | mesuré |
|---|---|---|---|
| `ressources.css` | `.atlas-defile` visible à TOUTES les largeurs (n'était affichée que sous 1024 px) | la colonne de contenu de `.res-corps` rend **752 px à 1440 comme à 1120** ; la matrice fait 1 046 px et l'explorateur SH6 860 px | **294 px et 108 px de colonnes cachées sur ORDINATEUR**, sans aucun signe. La colonne « Veille », que la note désigne nommément, n'était jamais visible |
| `ressources.css` | colonne collante étendue à `.atlas-sh6` | même défaut que la matrice, la 1re colonne porte le code SH6 | `position: sticky` confirmé sur les deux tableaux |
| `ressources.css` | `.res-corps p sub, sup` à 14 px sous 640 px | l'indice suit son parent : dans un chapô de 15 px il rendait 12,5 px | 12,5 → 14 px sur 23-research (+ en). Les indices d'un `.t-h2` (18 px) rendent déjà 15 px, non touchés |
| `ressources.css` | `.rang-boutons{align-items:center}` + `.lien-signal` min-height 40 / 44 px | un bouton de 40 px et un lien de texte dans la même rangée : le lien s'étirait et son texte se posait 16 px plus haut | 104 × 24 → 104 × 40 (desktop) et 104 × 44 (390 px) ; 117 × 24 sur le jumeau EN |
| `ressources.css` + 17-modeles | `.vignette.vignette-ico` remplace 6 `style=` inline identiques | grille en classe, jamais en inline | **sélecteur à deux classes obligatoire, mesuré** : à une seule, `.res-carte .vignette` (0,2,0) reprenait le fond et les 6 vignettes rendaient en gris clair, icône invisible. Corrigé et revérifié à l'image |
| `ressources.css` + 25-skills (+ en) | `.ligne-commande` : la rangée d'installation passe en classe ; le `$` quitte le bleu de signal pour l'encre atténuée | le `$` n'est pas une action, c'est de l'ornement, même raison que les 45 noms de skills | `$` à `rgba(255,255,255,0.52)`. **Préfixe `.sur-sombre` obligatoire sur le `code`, mesuré** : sans lui, `.sur-sombre .t-body` (0,2,0) gagnait et la commande tombait à blanc 0,64 au lieu du blanc plein qu'elle avait en inline |
| `ressources.css` | `.res-tete` sur UNE colonne sous 640 px | `composants.css:619` la remet sur deux colonnes sous 1024 px et rien ne la ramène ensuite | **2 × 157 px à 390 px sur les 11 pages qui la portent** ; le titre de 40 px débordait sa colonne, le chapô se lisait en trois mots par ligne → 334 px, titre sur une ligne |
| `ressources.css` | `.ligne-liste` passe à la ligne sous 640 px, `.principal` prend la largeur | flex `nowrap` sans règle d'écran étroit | largeur rendue de `.principal` à 390 px : **15-evenements 18 px**, 10-ressources 66, 18-recrutement 145, 23-research 179 → **294 px partout**. Hauteur de la ligne de 10-ressources 325 → 221 px |
| `ressources.css` + 15-evenements, 16-evenement | `.rang-meta` : la rangée « pastille + date », pastille en `flex:none` | la pastille « À venir » se comprimait et son libellé se coupait en deux lignes dans un rond | 66 px → pilule pleine, vérifié sur `15-evenements-m1.png` et `16-evenement-m1.png` |
| `ressources.css` | commentaire de la mesure de lecture recompté | il annonçait 82 caractères, extrapolés | 60-75 mesurés (tableau plus haut) |
| 12-article | note interne retirée : « Version française et anglaise du règlement conservées au dépôt. » | une note de dépôt servie au lecteur | la phrase utile au lecteur est conservée : « Les dates citées sont celles du texte publié, non d'une version consolidée. » |
| 12-article, 19-poste | `rgba(77,87,255,0.08)` et `(…,0.07)` → `var(--c-signal-wash)` | `#4D57FF` est le bleu du champ profond, ces deux blocs sont sur fond clair où le jeton vaut `#0008CF` | fond calculé `rgba(0,8,207,0.07)`. Plus aucun bleu en dur dans le territoire |
| 17-modeles | 6e carte : texte « US » → `ico:monde` | cinq cartes portaient une icône, la sixième un texte : deux anatomies dans une grille de six | `ico:monde` existe dans `commun/icones.js`. Une seule anatomie, vérifié à l'image |
| 16-evenement, 18-recrutement | titre bicolore retiré des h1 `t-titre-page` | `base.css:128-132` range explicitement « rencontres » et « recrutement » sous TITRE DE PAGE, pas sous HERO ; le bicolore n'appartient qu'au h1 d'un hero | 26-legal-data (+ en) reste le seul `t-hero` de la lane et garde son bicolore, à sa place |
| 13-glossaire | 6 pastilles de filtre inertes retirées | six `<span>` qui ne filtraient rien, exactement le motif retiré de 26-legal-data le même jour | la taxonomie n'est pas perdue : le fil d'Ariane d'une fiche la porte (« Glossaire · Acteurs » sur 14-terme). Le filtre est listé plus bas comme à construire |

**Effet hors de mes pages, assumé et contrôlé.** La règle `.ligne-liste` sous
640 px touche aussi 00-composants, 02-entreprise et 31-journal, qui portent le
même composant. Mesuré après : `.principal` rend 228 px sur 00-composants,
294 px sur 02-entreprise et 31-journal à 390 px ; à 1440 px rien ne bouge (la
règle est dans une media query). `.journal .ligne-liste` de la lane *cadre*
garde sa colonne : sa règle porte deux classes, elle l'emporte sur la mienne.
Capture de contrôle : **02-entreprise OK, 31-journal OK**.

---

## Ce qui reste hors territoire

**1. `commun/composants.css` (+ `commun/pied-en.html`), la seule alerte restante.**
Le lien « Terms » du pied anglais rend **37 × 44 px** : la largeur passe sous la
barre des 40. C'est l'unique alerte des 7 pages EN, et la seule de toute la lane.
Proposition précise, dans la media query d'écran étroit (vers la ligne 1344) :

```css
@media (max-width:640px){
  .pied-bas a{min-height:44px; min-width:44px; padding-inline:6px;
    display:inline-flex; align-items:center; justify-content:center}
}
```

**2. `construire.mjs`, fonction `resNav()` (ligne 651), deux défauts.**
La liste ne contient que cinq entrées (`Tout`, `Rencontres`, `Modèles`,
`Publications`, `Glossaire`). Or 23-research passe `<!--RES-NAV:Recherche-->` et
25-skills `<!--RES-NAV:Skills-->` : aucune des deux n'existe, donc **aucun élément
n'est marqué actif sur ces 4 pages** (vérifié à l'image sur `23-research-d1.png`
et `25-skills-d1.png`). Plus grave : 23-research-en et 25-skills-en reçoivent la
barre **en français** et pointant vers `/fr/`, vérifié dans `sortie/23-research-en.html` :

```html
<nav class="res-nav"><div class="titre">Ressources</div>
  <a href="/fr/resources">Tout</a> <a href="/fr/resources/events">Rencontres</a>
  <a href="/fr/resources/templates">Modèles</a> …
```

Proposition : donner à `resNav` un second argument de langue et deux listes, et
passer `p.en` au point d'appel (ligne 800).

```js
const resNav = (actif, en = false) => {
  const liens = en
    ? [['23-research-en.html','Research'],['24-blog-en.html','Blog'],
       ['25-skills-en.html','Skills'],['26-legal-data-en.html','Legal Data']]
    : [['10-ressources.html','Tout'],['15-evenements.html','Rencontres'],
       ['17-modeles.html','Modèles'],['11-blog.html','Publications'],
       ['13-glossaire.html','Glossaire'],['23-research.html','Recherche'],
       ['25-skills.html','Skills']]
  …
}
// ligne 800
corps = corps.replace(/<!--RES-NAV:([^>]*)-->/g, (_, a) => resNav(a.trim(), !!p.en))
```

**3. `commun/seo.json` : 6 pages de mon territoire sans fiche.**
Le build le dit à chaque passage : `pas de fiche SEO : 27-article-levee.html,
27-article-levee-en.html, 28-article-vivatech.html, 28-article-vivatech-en.html,
29-article-deel.html, 29-article-deel-en.html` → titre de repli « Cleo, <titre> ».
Ces six pages ont pourtant une route réelle (`/fr/blog/…` et `/en/blog/…` dans
`chemins.json`). À écrire : `titre` et `description` pour les six.
Au même endroit, `BreadcrumbList` n'est pas émis pour 10, 11, 12, 13, 14, 15, 16,
17, 18 et 19 (il manque `itemListElement`), ni `ItemList` pour 15-evenements.

**4. `commun/bandeau-nav-en.html`, `commun/pied-en.html`, `commun/chemins.json` :
les pages anglaises renvoient vers le français.**
Compté sur `sortie/23-research-en.html` : 16 liens `/fr/…` (`company` ×3,
`platform` ×3, `careers`, `changelog`, `customers`, `industries`,
`maria-action-plan`, `meet`, `resources`, `security`, `terms` ×2) contre 4 routes
`/en/`. Cinq pages seulement ont une route anglaise. Ce n'est pas un bug de
fabrication, c'est un trou de contenu : il faut soit arbitrer (assumer le renvoi
au français), soit produire les pages EN manquantes. À trancher avant mise en
ligne, en particulier pour « Terms » : une page anglaise renvoie vers des
conditions générales en français.

**5. `pages/00-composants.html` (ligne 115) : un échantillon `gc-deep` orphelin.**
La bibliothèque de composants affiche encore une vignette libellée « gc-deep,
canvas de dégradé ». Cette classe n'est plus définie nulle part dans le CSS chargé
(seulement dans `commun/composants-noir.css.sauvegarde`). L'échantillon rend une
boîte vide. À retirer, ou à redéfinir si le dégradé doit revivre.

**6. `pages/00-composants.html` : alertes préexistantes, non introduites par moi.**
La capture y relève 8 `href="#"`, 3 `input disabled`, 92 textes sous 14 px et un
paragraphe à 924 px. C'est une bibliothèque de composants, ces états sont sans
doute volontaires ; je le signale pour qu'on ne me les attribue pas. Mon unique
règle qui atteint cette page (`.ligne-liste` sous 640 px) laisse `.principal` à
228 px et ne change rien au desktop.

---

## Points de composition laissés en l'état, à arbitrer

Ce sont des jugements de DA, pas des défauts mesurables. Je ne les ai pas
tranchés seul.

- **27, 28, 29 (+ en) : le bandeau et le corps ne s'alignent pas.** Le bandeau
  `.article-tete` commence au bord du conteneur (x = 133 px à 1440), le corps
  `.article` est centré (bord gauche à x ≈ 403 px). 270 px d'écart. C'est
  antérieur à ce chantier (le corps était déjà `margin:0 auto`, à 760 px) mais la
  colonne étant passée à 580 px, l'écart est plus visible. Deux sorties :
  `.article{margin-left:0}` sur ces trois pages, ou centrer aussi le bandeau.
  12-article, 14-terme et 19-poste ne sont pas concernés : leur corps vit dans une
  grille avec sommaire ou encart.
- **26-legal-data garde `t-hero` (51,84 px)** quand les 24 autres pages sont à
  40 px. C'est la seule page de la lane qui ouvre sur une promesse avec deux
  appels à l'action, ce que la doctrine de `base.css` range sous HERO. Cohérent,
  mais c'est le seul écart de la famille.
- **25-skills : trois `<code>` portent une question et une réponse**, pas du code
  (« Mon shampoing est-il conforme en France ? », « Réponse sourcée · CPNP · EU
  1223/2009 »). Le rendu respecte la DA : `base.css:123` neutralise
  `code, kbd, samp, pre` en Satoshi et **aucun monospace n'a été trouvé dans le
  territoire**. Seul le balisage est approximatif.

---

## Contenu à produire avant mise en ligne

| page | ce qui manque | note de substitution servie |
|---|---|---|
| **18-recrutement** | les 3 intitulés de poste sont des exemples | oui, visible : « Maquette : les intitulés ci-dessous sont des exemples, à remplacer par les postes réels avant publication. » |
| **19-poste** | la fiche entière est un gabarit : intitulé, missions, cadre | oui, visible : « Maquette. Cette fiche est un gabarit… » |
| **22-legal** | les conditions générales sont un gabarit de mise en page | oui, visible : « Maquette. Ce texte est un gabarit de mise en page. Les conditions réelles doivent être rédigées et validées juridiquement avant publication. » |
| **17-modeles** | les 6 fichiers de modèle n'existent pas | oui, visible : « Ces modèles sont des maquettes. Les fichiers ne sont pas encore produits ». Le bouton envoie un courriel à `contact@cleolabs.co` : soit produire les fichiers, soit assumer la demande par courriel |
| **13-glossaire** | le filtre par famille est à construire. Les six familles existent (Acteurs, Documents, Étiquetage, Substances, Essais, plus « Tous ») et le fil d'Ariane d'une fiche les porte, mais aucune carte ne dit à quelle famille elle appartient | non : j'ai retiré les pastilles inertes plutôt que de servir une promesse d'interaction |
| **26-legal-data (+ en)** | l'explorateur montre **6 codes sur 5 613** et la matrice **9 types sur 17** | oui, visible et chiffré : « Affichés 6 sur 5 613 codes SH6 » et la note qui nomme les huit types à 0. Le listage réel et sa pagination sont à construire |
| **12-article, 14-terme, 16-evenement, 19-poste** | pages-spécimen : un article, un terme, un événement, un poste. Les collections réelles sont à produire | non, et elles n'en ont pas besoin : le contenu servi est réel et sourcé |
| **27, 28, 29 (+ en)** | fiches SEO absentes (titre et description) | non, invisible au lecteur mais visible au build |
| **pages EN** | 16 liens de navigation et de pied renvoient au français ; « Terms » et « Privacy policy » mènent à `/fr/terms` | non, à arbitrer (point 4 ci-dessus) |

Aucun chiffre, article, date, poste ni terme n'a été inventé dans cette session.
Les seules modifications rédactionnelles sont : le retrait d'une note interne sur
12-article (la phrase utile est conservée), le libellé « Demander le modèle » au
lieu de « Télécharger » sur 17-modeles (fait par la lane précédente, honnête
puisqu'aucun fichier n'existe), et `hello@` → `contact@` sur 22-legal (alignement
sur les 23 autres occurrences du site).

---

VERIFICATION: node construire.mjs -> 0 echec (47 page(s), tous les controles passent) ; node commun/capture2.mjs 10-ressources 11-blog 12-article 13-glossaire 14-terme 15-evenements 16-evenement 17-modeles 18-recrutement 19-poste 22-legal 23-research 23-research-en 24-blog 24-blog-en 25-skills 25-skills-en 26-legal-data 26-legal-data-en 27-article-levee 27-article-levee-en 28-article-vivatech 28-article-vivatech-en 29-article-deel 29-article-deel-en -> 18 pages OK, 7 alertes restantes (1 par page anglaise : `.pied-bas a` « Terms » a 37 x 44 px, hors territoire, dans commun/pied-en.html et commun/composants.css)
